
#include <bits/stdc++.h>
using namespace std;
int main()
{   unsigned long q,len, l, p,i,j;
    string s;
    cin>>s;
    cin>>q;
    len=s.length();

    while(q--)
    {
       cin>>l>>p;

    for(i=0;i<len;i++)
    {
        count2=0;
        for(j=i+1<j<len;j++)
        {
           if(s[i]==s[j] && )
        }
    }
    }

}
